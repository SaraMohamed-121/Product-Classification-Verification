#Product Classification Data
The Dataset for the classification part has 20 different products where each product has training folder and validation folder, the number of
training images for each product ranges from (6-11 images), while the number of validation images for each product ranges from (1-3 images).
  ➔In classification, you should train the model using all the training images belonging to the 20 different product classes.
    You should also report the validation accuracy on all the validation images.
    So, you should concatenate the validation sets from the 20 folders in this part.
